# A Pythonic Object

# 第十一章 — Pythonic 对象

《Fluent Python 2e》（Luciano Ramalho 著，O'Reilly，2020）第 11 章配套示例代码。

_memtest.py_ 脚本接收命令行传入的模块名并加载它。假设该模块定义了一个名为 `Vector` 的类，_memtest.py_ 会创建一个包含一千万个实例的列表，并报告创建列表前后的内存占用情况。
