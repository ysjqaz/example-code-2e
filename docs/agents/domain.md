# 领域文档

## 布局

单上下文（single-context）：

- `CONTEXT.md`（仓库根目录）——项目的领域模型与上下文说明
- `docs/adr/`——架构决策记录（ADR）

## 消费规则

- 需要理解项目领域时，先读取 `CONTEXT.md`。
- 涉及架构决策时，查阅 `docs/adr/` 下的 ADR 文件。
- 若 `CONTEXT.md` 不存在，可创建；更新后保持内容与代码一致。
- ADR 文件按编号命名（如 `0001-使用-asyncio-处理并发.md`），每条记录包含：背景、决策、后果。
